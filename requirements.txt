# Requirements for Ultralytics YOLO Money Detection System

# Core ML/Computer Vision
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Data handling and utilities
PyYAML>=6.0
matplotlib>=3.7.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# GUI (for Kivy app)
kivy>=2.2.0
kivymd>=1.1.0  # Optional for material design

# Development and logging
tqdm>=4.65.0
pytest>=7.4.0  # For testing
black>=23.0.0  # Code formatting

# Optional GPU support
# CUDA toolkit will need to be installed separately for NVIDIA GPUs
# For Apple Silicon, MPS support is built into PyTorch 2.0+

# Additional utilities
pathlib2>=2.3.7  # For Python < 3.8 compatibility
typing-extensions>=4.7.0